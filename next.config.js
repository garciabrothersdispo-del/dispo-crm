/** @type {import('next').NextConfig} */
const nextConfig = {
  // Capacitor/mobile: export static files
  // Uncomment the line below only when building for mobile:
  // output: 'export',
}

module.exports = nextConfig
